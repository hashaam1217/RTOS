#include "led.h"
#include "uart.h"
#include "timebase.h"

int main (void)
{
	led_init();
	uart_tx_init();
    timebase_init();

	int j = 0;

	while (1)
	{
		led_on();
        delay(1);
		led_off();
        delay(1);
		printf("STM32F446 - Hashaam Zafar %i  \n\r", j);
		j++;
	}
}
